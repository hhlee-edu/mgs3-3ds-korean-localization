# 핸드오프 2026-08-20

## 오늘 끝난 것

**stage/scenerio.gcx 전건 종결** — 1,571행, UNREVIEWED 0.
TRANSLATED 1,257 / KEEP_ENGLISH 131 / DONOR_MISCLASSIFIED 183 / HUMAN 0.
93,784 리소스 적용, gate PASS. staging 적용 완료.

**codec PERSONAL DATA 종결** — 47행 필드 구조 복원 + donor 오분류 2행 교정
(+1,358 location) + 공란 20행 번역(+160 location). 57행은 es/fr 분기라 정상 donor.
27,132 location 중 한국어 26,919 / 영어 213(es/fr). gate PASS, coverage 100%.

**저작권 정리** — 0.91에 들어갔던 게임 스크립트 49개 파일(14,644행)을
히스토리에서 제거하고 force push. `.gitignore` 규칙을 이름 패턴 → **전면 제외 +
허용 목록**으로 뒤집었다. 출처 이름은 저장소 전체에서 `대사집` /
`script reference` / `script_ref`로 치환했고 추적 파일에 0건.

**README 재작성** — 패치 README로. 3D 효과로 즐기려고 만든 AI 패치, 화자 구분은
영문 대사집 참고, 두 벌 빌드 규칙, 컨테이너별 범위.

## 현재 상태

```
git      f3493d8 (release/v0.83, pushed)
master   translation/10_master/current/codec.csv  sha 2770c3c02d38ebf8

staging  C:\Users\hhlee\Desktop\Romforge\output\unpacked\partition0
  romfs/codec.dat   e9026a5e8fe50358   (v0.93 빌드)
  romfs/movie.dat   c48d8cc807ea2b8e
  romfs/demo.dat    c44bb512a6a1d017
  romfs/stage/*     169개 적용 완료
  exefs/code.bin    4e693f32b1b20d99   (CPP-ON)
  exheader.bin      2bca5dcbae016722

builds   v0.93-codec-pd77 (최신 codec), v0.95-stage-full-verify (stage 검증본)
CCI      미생성
```

## 다음에 할 일 (2건)

### 1. 번역 데이터 통합 — 조사만 해둠, 실행 안 함

- `translation/10_master/current/`에 codec.csv 백업 **29개 251MB**(고유 23개).
  `translation/`은 gitignore라 **git 히스토리가 없다** — 이 .bak이 유일한 롤백
  수단이므로 삭제 대신 `archive/`로 이동 권장.
- stage authority가 아직 `translation/` 밖에 있다
  (`translation/50_local_evidence/2026-08-19-stage-pretranslation-analysis/stage-translation-working.csv`).
  `translation/10_master/current/stage.csv`로 옮기면 네 컨테이너가 한자리에 모인다.
- 목표 형태: `current/`에 codec·movie·demo·stage 4개 + `pending/runtime-corrections.csv`.

### 2. 최종 패치 두 벌 빌드 — 규칙만 문서화, 실물 없음

| 빌드 | 대상 | CPP |
|---|---|---|
| 구형 3DS용 | 구형 3DS / 2DS | OFF |
| New 3DS·에뮬용 | New 3DS / New 2DS / Azahar | ON |

현재 staging은 CPP-ON 한 벌. 여기서 CPP-OFF를 갈라내면 두 벌이 된다.
CPP 패치는 code.bin VA `0x0010AEF4`의 24바이트(6워드).
배포 형태(RomForge CCI / 실기 패치 파일)는 미정.

## 주의사항 (이번에 확인된 것)

- **staged `stage/*/scenerio.gcx`를 빌드 출력으로 덮어쓰지 말 것.** staged 파일은
  clean tree 파일 + EOF 상주 글리프 페이지이고, 페이지는 EOF-65,275에서 시작한다.
  `tools/mgs3d_stage_stage_to_romforge.py`로 splice해야 한다.
- **codec 용량은 record 단위 pooled 예산**이다. per-resource min slot 모델을 쓰면
  멀쩡한 번역을 축약하게 된다.
- **scanner의 `language` 열은 짧은 라벨에서 못 믿는다.** 인접 리소스를 직접 읽어
  분기를 확정할 것.
- **evidence CSV는 기본 제외.** 새 덤프를 커밋하려면 카운트/해시만 있는지 확인하고
  허용 목록에 추가해야 한다.
