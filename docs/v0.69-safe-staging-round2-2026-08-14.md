# v0.69 safe staging, round 2 (2026-08-14) — includes the pending-correction merge

Re-verified and re-staged codec/movie/demo after merging the 4 pending
corrections from `docs/v0.69-pending-corrections-and-history-fix-2026-08-14.md`
into `current/`. Same method as round 1
(`docs/v0.69-safe-staging-2026-08-14.md`): full capacity recheck against the
real build code, structural verification, archive-then-replace staging.
**No CCI built.**

## Capacity re-run against updated `current/`

| file | PASS | REVIEW | MUST_SHORTEN | vs round 1 |
|---|---|---|---|---|
| codec | 1,972 | 43 | 22 | GCX 2210 (SAVE menu fix) moved REVIEW→PASS |
| movie | 588 | n/a | 101 | unchanged |
| demo | 1,919 | n/a | 309 | **one new MUST_SHORTEN** |

## The one new problem: demo offset 11537816

The merged correction ("지금부터... 버추어스 미션을 개시한다.") is **2 bytes over**
its fixed 36-byte slot (needs 38). The line it replaced
("어 . 좋은 꿈을 꾸게 해 줘야지 .") fit only because it was short and *wrong*
content — the correct content is measurably longer than the slot allows.

**Not shortened here** (text-authoring decisions are the human translator's
call, not an AI guess -- see `pending/runtime-corrections.csv`'s own
convention). Resolution applied: excluded from this build's `demo-safe.csv`
(the derived, non-canonical build input only) — the subtitle falls back to
English for now rather than shipping the confirmed-wrong old Korean.
`current/demo.csv` keeps the correct (if oversized) Korean text unchanged;
`pending/runtime-corrections.csv` row marked `merged-oversized` with the
exact 2-byte deficit noted, so whoever authors the trim has the number
already computed.

The other 3 pending corrections build clean: movie offset 1064 (PASS, 9
bytes under a 20-byte budget), demo offset 12126700 (PASS, 9 bytes under a
44-byte budget), codec GCX 2210 (PASS, now 40 bytes under its 98,564-byte
GCX budget, freeing it from the REVIEW list).

## Build + verification (identical method to round 1)

All three rebuilt from the same clean-tree reference dats with 0 errors:

- codec: `build-korean --reuse-freed-font --preserve-record-layout`, 0
  Hangul glyphs added, size unchanged (67,204,976 B), 0/2,326 GCX with
  layout drift.
- movie/demo: `build-korean --fixed-layout-reclaim --static-allocation`, 0
  bytes appended font data, sizes unchanged (229,376 B / 772,935,680 B), 0
  subtitle-offset drift across 108/333 records.
- `audit-existing` safety gate: 0 conflicts, both movie and demo.

## Staged

`C:\Users\hhlee\Desktop\Romforge\output\unpacked\partition0\romfs\`:

| file | new sha256 |
|---|---|
| codec.dat | `9aab5464273a12afdf827708bef588e83bfc99eeacae6d1260fc0e5c2a11b50a` |
| movie.dat | `a4ffbf8efc764501114e0c77e31463d629ecffa14ea5a6c5b25dfa356c4dfc68` |
| demo.dat | `fe0124b9baa000c08aa9e3ec534afeacb91cf1735f150559be4b2b3ca49214a6` |

Round-1 staged files (2026-08-14, earlier) archived, not deleted, to
`C:\Users\hhlee\Desktop\Romforge\archive\pre-v0.69-safe-round2-20260814\`.
`stage/v000a_0/cache.hpk` (history-card fix) untouched by this round --
already staged in round 1.
